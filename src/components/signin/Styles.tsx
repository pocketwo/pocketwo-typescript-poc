import styled from 'styled-components/native';

const LogoView = styled.View`
  margin-top: 50px;
`;

const BackButtonView = styled.View`
  margin-top: 60px;
`;

const FormView = styled.View`
  margin-top: 30px;
`;

export {LogoView, BackButtonView, FormView};
