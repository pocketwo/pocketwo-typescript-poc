import styled from 'styled-components/native';

const FormView = styled.View`
  margin-top: 30px;
`;

const MobileNumberView = styled.View`
  margin-top: 10px;
`;

const AddressView = styled.View`
  margin-top: 20px;
`;

export {FormView, MobileNumberView, AddressView};
